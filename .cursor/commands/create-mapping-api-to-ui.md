# create-mapping-api-to-ui

As a senior developer, I want to create a mapping files:
1. From UI to API: Which screen or button will be used to display the data from the API?
2. From service to service: Which backend service will call which API? Which services? Which protocols?

Base on the SRS and SDD, create the mapping files.